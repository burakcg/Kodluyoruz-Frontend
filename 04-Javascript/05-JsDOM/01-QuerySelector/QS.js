const pickSelector = document.querySelector('p');
const pickSelector1= document.querySelector('.error');
const pickSelector2= document.querySelector('div.error');
console.log(pickSelector);

const pickSelectors =document.querySelectorAll('.error')
console.log(pickSelectors);
pickSelectors.forEach(selector =>{
    console.log(selector);
})